## Description:
---

* Module 4 Datasets, Arrays, Pointers Assignment.
* Create program that stores data sets into arrays, displays the data sets, and accesses a value via pointer.
* One .cpp file uses fstream to pull data from the included .csv, one has the data inputted into arrays in the code like the example on the homework site.
